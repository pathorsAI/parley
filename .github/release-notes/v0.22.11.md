Timeline fixes: the hover card is no longer clipped by the panel edge (portaled and viewport-aware, with tighter content), and clicking a dot now scrolls its finding into view in the list instead of only seeking the transcript
