The MCP server can now also remove a shared recording from an organization (delete_org_recording) — the last org operation that still required the app UI. Uploader or org admin/owner only; the personal original is untouched.
