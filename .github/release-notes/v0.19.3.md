### 修正

- **情報板不再把你自己講的話算成「對方」**：使用聲紋分離的會議（單一混音來源）中，抽取情報卡的 prompt 會把每一位發言者都標成對方，導致整場戰情誤判。現在改用與其他分析一致的發言者標籤。

### 幕後

- 新增 iOS app 的設計文件與 `ios/`（ParleyKit 逐字稿核心 + App 骨架）。iOS 版負責面對面會議的錄音與轉錄，深度分析仍以桌面版為主；尚未發布。
