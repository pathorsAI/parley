Removes live translation and the interpreter — they were effectively a separate product inside the meeting app, with their own windows and a bundled audio driver. Emoji are gone from the interface: icons where a mark carries information, plain text where the words already said it, and scenario icons are now a proper icon set instead of a typed-in emoji
