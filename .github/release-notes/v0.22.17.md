The MCP server can now manage the library end to end: create, rename and delete personal folders (mirrored to cloud sync), create and organize org folders, file org-shared recordings into them, and delete a recording outright. An external agent can finally organize the whole library — including the org side — without touching the app.
