{
  "name": "parley-templates-mcp",
  "version": "0.1.0",
  "description": "Local MCP server for managing Parley eval/TODO templates via the shared templates.json file.",
  "private": true,
  "type": "module",
  "bin": {
    "parley-templates-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25 || ^4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
